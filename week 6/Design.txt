Title:
    Countdown to christmas

Author(s):
    Jacob

Steps:
    1   create a global variable to store date and time of christmas
    2   create a function that will repeat once every second
        a   create a variable to store current date and time
        b   calculate the difference between the two dates
        c   return nothing
    3   create a function to calculate difference between two dates and times
        a   find the difference in time
        b   find the difference between the dates
        c   store both in an array
        d   return the array
    4   run the function from step 2